# Local environment — generated from azd outputs (`azd env get-values > .env`) plus the APIM key. Never commit.
.env
.env.*
!.env.example

# azd per-environment store (.azure/<env>/.env holds resolved config; not committed).
.azure/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# uv
.uv/

# OS / editor
.DS_Store
*.swp
